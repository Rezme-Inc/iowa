import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: "Iowa Fair Chance Hiring Law - Section 364.3(12)(a)",
  description: "Comprehensive compliance and assessment framework for Iowa Code section 364.3(12)(a) Fair Chance Hiring regulations and guidelines",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
